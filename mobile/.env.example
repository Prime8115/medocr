# Copy to .env. The backend base URL the app talks to.
# Physical device on the same wifi: use your computer's LAN IP, e.g. http://192.168.0.107:8080
# Emulator (Android): http://10.0.2.2:8080
EXPO_PUBLIC_API_URL=http://localhost:8080
